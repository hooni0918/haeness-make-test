__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.DS_Store

# --- harness runtime ---
# Ignore gate logs but keep the .claude/ harness itself tracked.
.claude/logs/*.log

# Layer 2 generation artifacts (skill candidates are reviewed, then installed
# into .claude/skills/ — the intermediate build output stays untracked).
build/
tools/v9/meta_prompt.candidate.md

# Layer 2 run telemetry (local feedback data for the self-improvement loop).
.claude/logs/v9_runs.jsonl
